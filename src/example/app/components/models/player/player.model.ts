import {SceneObject} from "../../../../../core/components/scene-object/scene-object";

export class PlayerModel extends SceneObject {
    constructor(options: { [key: string]: any }) {
        super(options);
    }

    public update() {

    }
}
